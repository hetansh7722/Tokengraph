## Install the Backend

tokengraph needs a Python backend to parse your codebase.

**Requirements:** Python 3.10+

**Recommended:** Install via [uv](https://docs.astral.sh/uv/):
```bash
uv pip install tokengraph
```

**Alternatives:**
```bash
pipx install tokengraph
# or
pip install tokengraph
```
